---
import LegalPageLayout from "../../www/layouts/LegalPageLayout.astro"

let baseUrl = import.meta.env.BASE_URL
---

<LegalPageLayout
	title="Seite nicht gefunden - Tilly"
	description="Die gesuchte Seite existiert nicht."
	locale="de"
>
	<h1>Seite nicht gefunden</h1>
	<p>Die gesuchte Seite existiert nicht.</p>
	<p>
		<a href={`${baseUrl}de/`}>Zur Startseite zurück</a> oder <a href={`${baseUrl}app`}>App öffnen</a>.
	</p>
</LegalPageLayout>